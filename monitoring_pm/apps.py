from django.apps import AppConfig


class MonitoringPmConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "monitoring_pm"
    verbose_name = "Monitoring Inventaris"
